{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API server with Node.js, Express, and Supabase",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --ignore node_modules/",
    "dev:watch": "nodemon server.js --watch config --watch controllers --watch middleware --watch models --watch routes",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Add ESLint configuration for code linting\"",
    "validate": "node -c server.js && echo \"✅ Server file is valid\""
  },
  "keywords": [
    "node",
    "express",
    "supabase",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
